export class newImage {

  public id: string;
  public name: string;
  public url: string;

  constructor(id: string, name: string, url: string) {
    // properties
    this.id = id;
    this.name = name;
    this.url = url;
  }
 }
